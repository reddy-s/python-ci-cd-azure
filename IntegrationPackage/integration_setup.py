from setuptools import setup, find_packages


setup(
    name='Integration',
    version='1.0.0',
    description='Sample Integration Package',
    packages=find_packages(),
    install_requires=[],
    zip_safe=True,
    include_package_data=True
)
