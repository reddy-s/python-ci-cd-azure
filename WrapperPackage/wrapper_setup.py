from setuptools import setup, find_packages


setup(
    name='Wrapper',
    version='1.0.0',
    description='Wrapper',
    packages=find_packages(),
    install_requires=[],
    zip_safe=True,
    include_package_data=True
)
