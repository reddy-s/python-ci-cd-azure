from setuptools import setup, find_packages


setup(
    name='Wrapper',
    version='',
    description='Wrapper',
    packages=find_packages(),
    install_requires=[],
    zip_safe=True,
    include_package_data=True
)
